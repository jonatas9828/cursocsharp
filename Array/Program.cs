{
    string[] carros = { "fusca", "corsa", "celta", "volvo"};
    int[] numeros = { 1, 2, 3, 4 };

    carros[0] = "brasilia amarela";

   // Console.WriteLine(carros.Length);


    for(int i = 0; i < carros.Length; i++)
    {
        Console.WriteLine(carros[i]);
    }
    
}
